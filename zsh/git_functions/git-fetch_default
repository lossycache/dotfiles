#!/bin/zsh
DEFAULT=$(git default_branch)
if [[ -z "$DEFAULT" ]]; then
  echo "Could not detect origin's default branch." >&2
  exit 1
fi
echo "📦 Detected default branch ${DEFAULT}"
git fetch origin "${DEFAULT}"
